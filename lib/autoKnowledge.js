// lib/autoKnowledge.js
// Minimal safe version (حتى لا ينهار النظام)
// تقدر ترجع لاحقاً لنسخة JSON الكبيرة

export function findRelevantIssues(text) {
  // رجّع مصفوفة فاضية حالياً
  // لاحقاً نربطها بملف data/auto_common_issues.json
  return [];
}
